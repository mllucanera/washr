    <div class="wrapper">
      <div class="icon-box-title">
        <img src="<%= asset_url('car.png') %>" alt="" class="icon-box-title">
      </div>
      <div class="box-inline-title">
        <p>Which car do you want to wash?</p>
      </div>
      <%= render '/shared/small_car_card_photo' %>
      <div id="booking">
        <% if @cars.empty? %>
        <div class="container" id="new-car-booking">
          <h2>Add your car:</h2>
          <%= render 'client/cars/form', car: @car, client: current_user %>
        </div>
        <% else %>
        <div class="center">
          <button type="button" class="button-opacity btn-modal" id="add-car-booking" data-toggle="modal" data-target=".bs-example-modal-lg"><img src="<%= asset_url('add.png') %>" alt="" class="icon-small"> Add new Car</button>
        </div>
        <div class="modal fade bs-example-modal-lg" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel">
          <div class="modal-dialog modal-lg container hidden" id="new-car-booking" role="document">
            <div class="modal-content">
              <%= render 'client/cars/form', car: @car, client: current_user %>
            </div>
          </div>
        </div>
        <% end %>
        <div class="container">
<!--     <%= simple_form_for @booking do |f| %>
    <%= f.button :submit %>
    <%= f.error :car %>

    <%= f.input :car_id, collection: @cars, as: :radio_buttons %>
    <%= f.hidden_field :address, value: @booking.address %>
    <% end %> -->
  </div>
</div>
</div>
