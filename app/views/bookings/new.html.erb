    <%= render '/shared/small_car_card_photo' %>

<div id="booking">
  <% if @cars.empty? %>

  <div class="container" id="new-car-booking">
    <h2>Add your car:</h2>
    <%= render 'client/cars/form', car: @car, client: current_user %>
  </div>
  <% else %>
  <div class="center">
    <%= link_to "#", class: "button-opacity", id: 'add-car-booking' do %>
    <i class="fas fa-plus-circle"></i>
    Add New Car
    <% end %>
  </div>
  <div id="new-car-booking" class="container hidden">
    <%= render 'client/cars/form', car: @car, client: current_user %>
  </div>
  <% end %>
  <div class="container">
<!--     <%= simple_form_for @booking do |f| %>
    <%= f.button :submit %>
    <%= f.error :car %>

    <%= f.input :car_id, collection: @cars, as: :radio_buttons %>
    <%= f.hidden_field :address, value: @booking.address %>
    <% end %> -->
  </div>
</div>
